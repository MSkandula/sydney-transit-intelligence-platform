name: transit_intelligence
version: "1.0.0"
config-version: 2

profile: transit_intelligence

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  transit_intelligence:
    staging:
      +materialized: view
    marts:
      +materialized: table
